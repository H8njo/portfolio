---
title: 프론트가 평문 비밀번호도 못 만지게 한 보안 포털 BFF
summary: 프론트와 사내 보안 API 사이에 NestJS 한 겹을 끼웠다. 그 한 겹이 보안 경계가 됐고, 라운드트립·바이너리·인코딩까지 같이 흡수했다.
featured: false
order: 9
demo: false
tags: [보안, API, 인증]
metrics:
  - label: 단독 기여
    value: 프론트+백 199 커밋
  - label: 동시 연결 DB
    value: PostgreSQL + MongoDB
  - label: BFF 집계
    value: 10개 호출 → 1응답
date: 2026-06-08
---

{/* [확인] 표시는 NDA 공개범위 정한 뒤 실제 수치/표현으로. */}

평문 비밀번호가 프론트엔드 코드 어딘가에 잠깐이라도 머무는 게 싫었다.

EDR 보안 관제 포털을 만들었다. 보안 분석가가 위협 인시던트를 티켓으로 받아 악성코드·IP·해시를 분석하고, 그 자리에서 URL이나 방화벽을 차단하는 사내 관제 시스템이다. 프론트(Next.js)부터 백엔드(NestJS)까지 같은 기능을 양쪽에서 혼자 끝까지 짰다. 그래서 한 기능을 두 시점으로 봤다. "이건 프론트에서 처리할 일인가, 아닌가"를 매번 물어야 했다.

## 문제

프론트는 사내 TOS(위협 운영 시스템) API를 직접 부르면 안 됐다. 직접 부르는 순간 인증 토큰을 클라이언트가 들고 있어야 하고, 비밀번호를 프론트에서 해시해야 하고, 권한 필터를 클라이언트 코드가 판단해야 한다. 보안 시스템에서 그건 전부 위조 가능한 자리다. 프론트가 보내는 건 다 의심해야 하는데, 보안 로직까지 거기 두면 의심할 게 너무 많아진다.

## 트레이드오프 결정

프론트와 TOS 사이에 NestJS BFF 한 겹을 끼웠다. 호출이 한 단계 늘고, 같은 도메인 로직을 양쪽에서 신경 써야 하는 비용이 있다. 그걸 감수하기로 했다. 그 한 겹이 보안 경계가 되기 때문이다.

- **비밀번호는 BFF에서 해시한다.** 프론트는 평문 old/new를 보내고, BFF가 `crypto.SHA256()`을 돌린 뒤 TOS로 넘긴다. 프론트 코드는 해시 함수조차 모른다.
- **사용자 신원은 BFF가 주입한다.** 티켓 생성 시 `userId`를 프론트가 보내는 게 아니라, JWT에서 뽑은 `@User()`로 서버가 끼워 넣는다. 클라이언트가 남의 ID를 사칭할 자리를 없앴다.
- **권한 분기도 서버에서 한다.** 분석가(AIR 계정)는 전체 티켓, 일반 사용자는 자기 테넌트만. `siteType`으로 쿼리를 컨트롤러에서 갈랐다. 프론트가 "전체 줘"라고 해도 서버가 거른다.

## 접근

끼운 한 겹은 보안만 한 게 아니라 비용을 자산으로 돌려줬다. 대시보드 하나 그리는 데 내 티켓·전체 티켓·보안등급·에이전트 설치율·차단 현황이 다 필요한데, 이걸 프론트가 10번 따로 부르게 두지 않았다. BFF가 `Promise.all`로 병렬로 모아 한 응답으로 내려준다. 인시던트 상세도 IP·해시·이벤트 로그를 서버에서 합쳐서 준다. 호출 한 단계가 늘어난 대신 줄줄이 나가던 호출을 BFF가 대신 먹어줬다.

파일과 인코딩도 BFF가 흡수했다. 첨부파일 다운로드는 TOS → NestJS → axios → 브라우저 4단계를 거치는데, 각 단계의 파일 전달 방식이 하나라도 어긋나면 파일이 깨져 안 열린다. 한 군데만 보면 안 잡혀서 제일 짜증났다. 네 단계를 한 줄로 꿰어 스트림으로 흘려보내고 나서야 열렸다. 업로드는 반대로 한글 파일명이 깨졌다. multer가 받은 파일명 인코딩이 어긋나서, 인코딩을 바로잡아 원래 이름으로 복원했다. 둘 다 프론트는 파일을 그냥 던지기만 한다.

```ts
@Post(':ticketId/attachments/:fileId/download')
async download(@Param('fileId') fileId, @Res({ passthrough: true }) res) {
  const stream = await this.upstream.downloadFile(fileId); // responseType: 'stream'
  res.set({ 'Content-Type': 'application/octet-stream;charset=UTF-8' });
  return new StreamableFile(stream);
}
```

데이터는 한 종류가 아니라 DB를 둘로 쪼갰다. TOS가 내려주는 조회 전용 정형 뷰는 PostgreSQL(TypeORM), 스키마가 계속 바뀌는 차단·알림은 MongoDB(Mongoose)로 보내고, 한 NestJS 서비스에서 `@InjectRepository`와 `@InjectModel`을 같이 주입받아 한 흐름에서 엮었다.

## 결과

프론트는 평문 비밀번호도, TOS 토큰도, 권한 판단도, 파일명 인코딩도 만지지 않는다. 그 책임이 전부 BFF 한 곳에 모였다. 보안 로직이 한 군데 있으니 의심할 표면이 좁아졌고, 고칠 때도 한 곳만 본다. 덤으로 대시보드와 인시던트 상세는 호출 한 번으로 그려지고, 파일은 한글 이름이든 바이너리든 안 깨진다.

가장 만족스러운 건 따로 있다. 같은 기능을 프론트와 백 양쪽에서 짜다 보니, "이 로직이 어느 쪽에 살아야 하는가"를 계속 결정해야 했다. 그 결정의 기준이 거의 다 보안이었고, 그래서 거의 다 백엔드로 갔다.

## 회고

BFF를 끼우면 같은 도메인을 양쪽에서 신경 써야 한다는 비용이 분명히 있다. 그런데 보안 시스템에선 그 비용이 오히려 자산이었다. 경계가 명확하니 "여긴 못 믿는 입력, 여기서부턴 검증된 값"이 코드 구조로 드러났다.

다시 한다면 refresh 토큰을 서버 메모리 Map이 아니라 영속 저장소에 두고 매칭을 제대로 구현했을 거다. 그때는 "추후 구현" 주석으로 남겨두고 메모리로 갔는데, 재시작하면 날아가는 구조였다. 그 외엔 [확인].
